target_x = x;
target_y = y;

//There's an event called alarm[0] that we code to run 60 frames after the start, and for every 60 frames after.
alarm[0] = 60;

tilemap = layer_tilemap_get_id("Tiles_Col");